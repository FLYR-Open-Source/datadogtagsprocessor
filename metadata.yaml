display_name: Datadog DDTags Processor
description: Moves or merges resource and span/log attributes into the `ddtags` attribute for Datadog.
type: datadogtags

github_project: FLYR-Open-Source/datadogtagsprocessor

status:
  class: processor
  stability:
    alpha: [traces, logs]
  codeowners:
    active: [vspallas-flyr]

tests:
  config:
